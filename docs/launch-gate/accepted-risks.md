# Accepted risks

The accepted-risk record `qera-system/gates/launch-gate.md` requires, in its format. A row here is a floor item knowingly shipped unfixed. Resolve-by points are milestones, not calendar dates: the project has no fixed launch date (decided 2026-09-26). Anything past its resolve-by point is no longer an accepted risk: it is an unfixed defect with paperwork, and the next gate run treats it as a NO-GO.

**Gate owner:** Shivanshu Pareek
**Override authority:** Shivanshu Pareek

One person holds both roles, which the gate allows at this size provided the second is a conscious act. Accepting a row below is that act: it is signed by the override authority, not assumed by whoever is shipping.

| Date       | Item                                                                                                                    | Why acceptable for _this_ launch                                                                                                                                                                                                                                                                                          | Owner            | Resolve by                                                                                                 | Resolved |
| ---------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------- | -------- |
| 2026-09-26 | Homepage pricing cards show placeholder prices and features (`src/content/pricing.ts`, every row)                       | Decided 2026-09-26. The figures are the brief's, not confirmed by the client. `/pricing` stays `noindex` until they are, so no search or answer engine quotes them; the homepage cards are the remaining exposure.                                                                                                        | Shivanshu Pareek | Before launch. The site does not go live quoting unconfirmed prices                                        |          |
| 2026-09-26 | Process rail does not meet WCAG 1.4.10 Reflow (two-dimensional scrolling for reading content at 320 CSS px / 400% zoom) | Signed-off design decision (AGENTS.md "Accessibility — 1.4.10 is knowingly not met"). Mitigated: focusable `role="group"` scroller with a label, six `<h3>` in a real `<ol>` that read linearly, and `prefers-reduced-motion` removes the pin. Not mitigated: a single-axis reading path for a sighted user at 400% zoom. | Shivanshu Pareek | Review six months after launch. A signed-off design choice, so this is a review, not a fix                 |          |
| 2026-09-26 | CSP allows `'unsafe-inline'` in `script-src` and `style-src` (`next.config.ts`)                                         | Decided 2026-09-26: keep the static policy. Next.js emits inline scripts and styles, and a nonce-based policy makes every page dynamic. Since Phase 2 a stricter `Content-Security-Policy-Report-Only` twin (no `'unsafe-inline'`) reports to `/api/csp-report`, so what enforcing it would break is measured first.      | Shivanshu Pareek | 30 days after the Phase 2 report-only policy ships, once there is data on what a strict policy would break |          |
