# The gate. Every job here is a required status check on `latest` and `main`
# (AGENTS.md "Testing policy" lists the exact branch-protection settings). A red
# job is reported with its output and fixed at the cause; it is never skipped,
# retried into green, or worked around by weakening an assertion.

name: CI

on:
  pull_request:
    branches: [latest, main]
  push:
    branches: [latest, main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NEXT_TELEMETRY_DISABLED: "1"

jobs:
  static:
    name: static
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # Security System §12: no known high or critical advisory in what ships.
      # --prod: the dev-only tree (Lighthouse CI's dependencies) never reaches a
      # visitor; its advisories are tracked in docs/runbooks/compromised-dependency.md.
      - name: Audit shipped dependencies
        run: pnpm audit --prod --audit-level high
      - name: Lint
        run: pnpm lint
      - name: Typecheck
        run: pnpm typecheck
      - name: Production build
        run: pnpm build
      - name: Cascade layer order in every chunk
        run: node scripts/assert-layer-order.mjs

  unit:
    name: unit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          # The ratchet and the touched-file rule compare against the base, so
          # the base commit has to be in the clone.
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Resolve the base to compare against
        id: base
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "ref=origin/${{ github.base_ref }}" >> "$GITHUB_OUTPUT"
          elif [ -n "${{ github.event.before }}" ] && [ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]; then
            echo "ref=${{ github.event.before }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=" >> "$GITHUB_OUTPUT"
          fi
      - name: Unit tests with coverage
        run: pnpm test:coverage
      - name: Coverage gate
        run: |
          if [ -n "${{ steps.base.outputs.ref }}" ]; then
            node scripts/coverage-gate.mjs --base "${{ steps.base.outputs.ref }}"
          else
            node scripts/coverage-gate.mjs
          fi
      - name: Test ratio
        run: |
          if [ -n "${{ steps.base.outputs.ref }}" ]; then
            node scripts/test-ratio.mjs --base "${{ steps.base.outputs.ref }}"
          else
            node scripts/test-ratio.mjs
          fi
      - name: Ratio summary
        if: always()
        run: |
          {
            echo '## Test ratio'
            echo '```'
            node scripts/test-ratio.mjs ${{ steps.base.outputs.ref && format('--base {0}', steps.base.outputs.ref) || '' }} || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: coverage/
          retention-days: 14

  e2e:
    name: e2e (${{ matrix.project }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        project: ["Desktop Chrome", "iPhone 17", "Galaxy S24"]
    env:
      CI: "true"
      # No Resend secrets here. Form tests write their emails to the e2e mail
      # sink (src/app/contact/actions.ts), since sending them for real
      # exhausted the account's monthly quota on 2026-09-26. The delivery job
      # below is the one place a real email is sent.
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install browsers
        run: pnpm exec playwright install --with-deps chromium webkit
      - name: Playwright
        run: pnpm exec playwright test --project "${{ matrix.project }}"
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-${{ strategy.job-index }}
          path: |
            playwright-report/
            test-results/
          retention-days: 14

  # One real email through Resend per run. Not a required check, by decision:
  # it fails whenever Resend refuses a send (quota, key, domain), a fact about
  # the account rather than the code under review. Visible on every PR.
  delivery:
    name: delivery
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CI: "true"
      E2E_REAL_MAIL: "1"
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      LEAD_EMAIL: ${{ secrets.LEAD_EMAIL }}
      LEAD_FROM_EMAIL: ${{ secrets.LEAD_FROM_EMAIL }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: One real email through Resend
        run: pnpm exec playwright test --project delivery
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-delivery
          path: |
            playwright-report/
            test-results/
          retention-days: 14

  e2e-extra:
    name: e2e-extra
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CI: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: Forced colours and 200% zoom
        run: pnpm exec playwright test --project forced-colors --project zoom-200
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-extra
          path: |
            playwright-report/
            test-results/
          retention-days: 14

  lighthouse:
    name: lighthouse
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Production build
        run: pnpm build
      - name: Lighthouse CI
        run: pnpm test:lhci
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lighthouse
          path: .lighthouseci/
          retention-days: 14
